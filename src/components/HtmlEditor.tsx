import React from 'react';

// draft.js based editor and previewer
// ideally, each element root with its classes is displayed on the left of the document
// No display of 
export class HtmlEditor extends React.Component {
    render(){
        return <p>epub html</p>
    }
}